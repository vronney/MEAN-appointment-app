import { MatTableReponsiveDirective } from './mat-table-reponsive.directive';

describe('MatTableReponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTableReponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
